{
  "name": "dig-website",
  "version": "0.1.0",
  "private": true,
  "description": "Data Infrastructure Group (DIG) website built with Eleventy",
  "scripts": {
    "dev": "ELEVENTY_ENV=development npx @11ty/eleventy --serve",
    "build": "ELEVENTY_ENV=production npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "luxon": "^3.7.2"
  }
}
